package androidApi.repository;

import androidApi.model.Roomtypes;
import org.springframework.data.jpa.repository.JpaRepository;

public interface RoomtypesRepository extends JpaRepository<Roomtypes,Integer> {
}
